class Calculator {

    add(x, y) {
        return x + y;
    };

    sub(x, y) {
        return x - y
    };

    multiply(x, y)  {
        return x * y;
    };

    divide(x, y)  {
        return x / y;
    };
};

module.exports = Calculator;